[package]
name = "db"
version = "0.1.0"
edition = "2024"

[dependencies]
types = { path = "../types" }
sqlx = { version = "0.9", features = ["runtime-tokio", "postgres", "tls-rustls", "chrono", "uuid"] }